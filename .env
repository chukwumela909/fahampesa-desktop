# Backend API base for all builds (dev + prod). `.env.production` overrides this for
# `vite build` / `tauri build`, but the value is the same deployed backend.
VITE_API_BASE_URL=https://fahampesa.amenviron.app/api/v1
